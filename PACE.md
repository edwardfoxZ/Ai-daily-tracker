# Pace

Your per-user habit coach lives in **Messages** (pinned first conversation).

Backend routes: `/api/agent/*`
Set `GROQ_API_KEY` in `chainpace-backend/.env`.

Pull latest master, restart Go + Next, then open Messages.
